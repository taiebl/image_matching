
#pragma once

#include "matcher.h"



Matcher::Matcher()
{

}

void Matcher::readImage(const std::string & imageName)
{
	auto it = std::find(imageList.begin, imageList.end, imageName);
	
	if (it == imageList.end())
	{
		imageList.push_back()
	}
	else
	{
		imageList.insert
	}
}

void Matcher::run()
{

}